{"id": "symbol_memtable", "category": "symbol_grounding", "prompt": "Find where the MemTable is implemented in LevelDB. Show the main source file, the write path into it, and the most relevant tests. Explain the relationship between them.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "symbol_sstable_io", "category": "symbol_grounding", "prompt": "Locate the implementation of the table / SSTable reader and writer in LevelDB. Identify the central files and explain how they fit together.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "symbol_compaction", "category": "symbol_grounding", "prompt": "Find where compaction is implemented in LevelDB. Show the most important classes and source files involved.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "usage_put_path", "category": "find_usages", "prompt": "Trace the call chain for a normal DB::Put operation in LevelDB. Start from the public API and walk through the main internal path until the write reaches the MemTable and log.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "usage_get_path", "category": "find_usages", "prompt": "Trace how a DB::Get operation works internally in LevelDB. Start from the public API and walk through the main path through MemTable, immutable MemTable, and SSTables until a value is found or not found.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "usage_background_compaction", "category": "find_usages", "prompt": "Trace how LevelDB decides to perform a background compaction. Identify the key classes, scheduling points, and the main internal path.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "tests_compaction_behavior", "category": "find_tests", "prompt": "I want to understand compaction behavior in LevelDB. Find the most relevant production files and the best tests that document tricky edge cases. Summarize the edge cases covered by the tests.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "tests_wal_recovery", "category": "find_tests", "prompt": "Find the best tests covering recovery from the write-ahead log in LevelDB and explain what scenarios they validate.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "tests_snapshot_consistency", "category": "find_tests", "prompt": "Find tests for snapshot behavior in LevelDB and explain what consistency guarantees they demonstrate.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "arch_leveldb_overview", "category": "architecture", "prompt": "Explain how LevelDB is organized at a high level. Identify the main public interfaces, key implementations, and the major storage layers such as log, MemTable, versioning, table files, and compaction. Point me to the most central files.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "arch_read_path", "category": "architecture", "prompt": "Give me a high-level map of LevelDB’s read path. Group the important classes by role: public API, in-memory lookup, version metadata, table lookup, and cache support.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "arch_version_compaction", "category": "architecture", "prompt": "Explain the package structure around LevelDB’s version and compaction subsystem. Which files look foundational, and which are utilities layered on top?", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "negative_mvcc_subsystem", "category": "negative_control", "prompt": "Does LevelDB implement MVCC as a named public subsystem in the repository? Verify in code and say clearly if that exact term or subsystem does not exist.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
{"id": "negative_lsm_tree_manager", "category": "negative_control", "prompt": "Find a class named LSMTreeManager in LevelDB. If it does not exist, say so clearly and show the closest real classes instead.", "prompt_suffix": "Ground your answer in the repository. Name the exact files and symbols you used, and say clearly when something is not found."}
